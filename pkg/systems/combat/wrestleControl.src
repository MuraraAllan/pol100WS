use uo;
use os;
use cfgfile;

program itemcontrol(item)
  var cfgfile  := ReadConfigFile(":combat:itemdesc");
  var dmg      := cfgfile[item.objtype].damage;

   /// item.hitscript := "mainHitScript";
Broadcast("Wrestling hit control");
endprogram